export type ServerInfo = {
  qualifiedName: string,
  displayName: string,
  description: string,
  iconUrl?: string,
  verified: boolean,
  useCount: number,
  remote: boolean,
  createdAt: string,
  homepage: string
}
